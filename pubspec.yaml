name: tactical_connect
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.3
  go_router: ^18.0.1
  drift: ^2.22.1
  sqlite3_flutter_libs: ^0.6.0+eol
  path_provider: ^2.1.5
  path: ^1.9.0
  # freezed_annotation: ^2.4.4
  # json_annotation: ^4.9.0
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  geolocator: ^14.0.3
  # Disabled for now: nothing uses it yet, and its Android plugin needs
  # compileSdk 37, which the current Android Gradle Plugin does not support.
  # permission_handler: ^13.0.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  drift_dev: ^2.22.1
  build_runner: ^2.4.13
  # freezed: ^2.5.7
  # json_serializable: ^6.9.0
flutter:
  uses-material-design: true
